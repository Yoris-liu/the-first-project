<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hngc.mapper.ProductDao">
    <select id="findAll" parameterType="com.hngc.pojo.Product"  resultType="com.hngc.pojo.Product">
        select * from product where 1=1
        <if test="product_id!=null and product_id!=''">
            and product_id=#{product_id}
        </if>
        <if test="product_name!=null and product_name!=''">
            and product_name like concat('%',#{product_name},'%')
        </if>
        <if test="product_code!=null and product_code!=''">
            and product_code like concat('%',#{product_code},'%')
        </if>
        <if test="category_id!=null and category_id!=''">
            and category_id=#{category_id}
        </if>
        <if test="status!=null and status!=''">
            and status=#{status}
        </if>
    </select>
    <select id="findById" parameterType="Integer" resultType="com.hngc.pojo.Product">
        select * from product where product_id=#{id}
    </select>
    <insert id="save" parameterType="com.hngc.pojo.Product">
        insert into product(product_name,product_code,category_id,subtitle,price,original_price,stock_num,status,image_url,sales_num,create_time,update_time)
        value(#{product_name},#{product_code},#{category_id},#{subtitle},#{price},#{original_price},#{stock_num},#{status},#{image_url},#{sales_num},#{create_time},#{update_time})
    </insert>
    <delete id="delete" parameterType="Integer">
        delete from product where product_id=#{id}
    </delete>
    <update id="update" parameterType="com.hngc.pojo.Product">
        update product
        <trim prefix="set" suffixOverrides="," suffix="where product_id=#{product_id}">
            <if test="product_name!=null and product_name!=''">
                product_name=#{product_name},
            </if>
            <if test="product_code!=null and product_code!=''">
                product_code=#{product_code},
            </if>
            <if test="category_id!=null and category_id!=''">
                category_id=#{category_id},
            </if>
            <if test="subtitle!=null and subtitle!=''">
                subtitle=#{subtitle},
            </if>
            <if test="price!=null and price!=''">
                price=#{price},
            </if>
            <if test="original_price!=null and original_price!=''">
                original_price=#{original_price},
            </if>
            <if test="stock_num!=null and stock_num!=''">
                stock_num=#{stock_num},
            </if>
            <if test="status!=null and status!=''">
                status=#{status},
            </if>
            <if test="image_url!=null and image_url!=''">
                image_url=#{image_url},
            </if>
            <if test="sales_num!=null and sales_num!=''">
                sales_num=#{sales_num},
            </if>
            <if test="create_time!=null and create_time!=''">
                create_time=#{create_time},
            </if>
            <if test="update_time!=null and update_time!=''">
                update_time=#{update_time},
            </if>
        </trim>
    </update>
</mapper>